require 'toggleterm'.setup({
    size = 20,
    autochdir = true,
    hide_numbers = true,
    direction = 'float',
    shell = 'fish -l',
    start_in_insert = true,
    insert_mappings = false,
    terminal_mappings = true,
})
